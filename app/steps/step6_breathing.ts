import { SimulationToolbox } from '../types/simulation';

export const step6OxygenBreathing = async (tools: SimulationToolbox) => {
  const { addHeader, addEMT, waitForContinue, getUserInput, dispatch } = tools;
};